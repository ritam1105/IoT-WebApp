# IoT + WebApp
# Dowloads:
dowload firebase node.js key from settings and paste it in your vs Code and named it as serviceAccountKey.json
also creates an .env file to store FireBase_URL and MONGO_URL 